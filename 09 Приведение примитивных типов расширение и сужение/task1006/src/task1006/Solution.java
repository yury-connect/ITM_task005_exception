package task1006;

/* 
Убери ненужные операторы приведения типа, чтобы получился ответ: result: 1000.0.
double d = (short) 2.50256e2d;
char c = (short) 'd';
short s = (short) 2.22;
int i = (short) 150000;
float f = (short) 0.50f;
double result = f + (i / c) - (d * s) - 500e-3;


Requirements:
1. Программа должна выводить текст на экран.
2. Нельзя менять команду вывода на экран.
3. Метод main() должен содержать переменную c типа char.
4. Метод main() должен содержать переменную s типа short.
5. Метод main() должен содержать переменную i типа int.
6. Метод main() должен содержать переменную f типа float.
7. Метод main() должен содержать переменную d типа double.
8. Метод main() должен содержать переменную result типа double.
9. Начальное значение переменных при инициализации менять нельзя. Можно добавлять только операторы приведения типа.
10. Программа должна выводить текст "result: 1000.0".*/


public class Solution {
    public static void main(String[] args) {
        double d = (short) 2.50256e2d;  // Приведение к short оставляем
        char c = 'd';  // Убрали приведение к short
        short s = (short) 2.22;  // Приведение оставляем, так как 2.22 не может быть short
        int i = 150000;  // Убрали приведение к short
        float f = 0.50f;  // Убрали приведение к short
        double result = f + (i / c) - (d * s) - 500e-3;
        System.out.println("result: " + result);  // Ожидаемый вывод: 1000.0
    }
}

/*
Исходный код
    public static void main(String[] args) {
        double d = (short) 2.50256e2d;
        char c = (short) 'd';
        short s = (short) 2.22;
        int i = (short) 150000;
        float f = (short) 0.50f;
        double result = f + (i / c) - (d * s) - 500e-3;
        System.out.println("result: " + result);
    }

    Задание:
Программа должна выводить result: 1000.0, убрав ненужные приведения типов. Разберем каждую переменную и её влияние на итоговый результат.

    Анализ и правки:

    double d = (short) 2.50256e2d;
2.50256e2d — это 250.256, при приведении к short это округляется до 250. Это приведение нужно оставить, так как оно влияет на точность.

    char c = (short) 'd';
'd' — это символ с числовым значением 100 в кодировке Unicode. Приведение к short здесь не нужно, так как char уже может хранить это значение. Убираем приведение.

    short s = (short) 2.22;
Приведение 2.22 к типу short обрезает дробную часть, оставляя 2. Приведение нужно оставить, так как short не может напрямую хранить дробные числа.

    int i = (short) 150000;
Приведение 150000 к short приводит к значению -31072 (так как 150000 выходит за пределы диапазона short). Приведение нужно убрать, чтобы сохранить значение 150000.

    float f = (short) 0.50f;
Приведение 0.50f к short округляется до 0. Приведение нужно убрать, чтобы сохранить исходное значение 0.50f.

    Окончательный код:
public class Solution {
    public static void main(String[] args) {
        double d = (short) 2.50256e2d;  // Приведение к short оставляем
        char c = 'd';  // Убрали приведение к short
        short s = (short) 2.22;  // Приведение оставляем, так как 2.22 не может быть short
        int i = 150000;  // Убрали приведение к short
        float f = 0.50f;  // Убрали приведение к short
        double result = f + (i / c) - (d * s) - 500e-3;
        System.out.println("result: " + result);  // Ожидаемый вывод: 1000.0
    }
}

Объяснение результата:
d = 250 (результат приведения 250.256 к short).
c = 100 (код символа 'd').
s = 2 (результат приведения 2.22 к short).
i = 150000 (значение сохранено как есть).
f = 0.50f (значение без приведения).

 */